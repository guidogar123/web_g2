---
phase: 05-performance-launch-validation
task: 9
total_tasks: 15
status: in_progress
last_updated: 2026-04-04T01:12:10.863Z
---

<current_state>
EasyPanel deployment is live and correct. The Next.js app is running on the EasyPanel VPS (82.25.86.23) behind Traefik, serving correctly on port 80. DNS has been updated by the user to point g2intelligence.co → 82.25.86.23. However, the browser (Chrome MCP + likely user's browser) still has the old DNS cached (72.62.87.218 = old Hostinger nginx server). Once DNS cache is cleared, the site should show the new Next.js app correctly.
</current_state>

<completed_work>

- Dockerfile built for Next.js standalone output, PORT=80, NEXT_PUBLIC webhook URL baked in (commits 23abc5e → d219816)
- public/ SVG assets committed (were missing, caused Docker build failure)
- Docker image built on VPS: `easypanel/posgresql/web_g2:latest` from /tmp/web_g2_build (cloned from GitHub)
- Docker Swarm service `posgresql_web_g2` force-updated to new image
- Diagnosed that g2intelligence.co DNS pointed to old server (72.62.87.218)
- User updated DNS A record → 82.25.86.23 confirmed via Google DNS
- Verified: container 2c3bc510ccbc serving correct Next.js HTML internally on port 80
- Verified: Traefik config correctly routes g2intelligence.co → posgresql_web_g2:80
- Verified: overlay network connectivity works (alpine container can GET posgresql_web_g2:80 → correct Next.js HTML)
</completed_work>

<remaining_work>

- Task 9: Verify site renders correctly after DNS cache cleared (user action needed first)
- Task 10: Compare visual parity with Vercel reference site
- Task 11: Run Lighthouse / Core Web Vitals on production URL
- Task 12: Test n8n contact form end-to-end in production
- Task 13: Test n8n schedule form end-to-end in production
- Task 14: Test chat widget connects to n8n in production
- Task 15: Set up EasyPanel auto-deploy from GitHub webhook (optional but recommended)
</remaining_work>

<decisions_made>

- Deployed via SSH + manual docker build on VPS (EasyPanel API returns 401, password required)
- PORT=80 in Dockerfile because Traefik routes to :80
- NEXT_PUBLIC_N8N_CHAT_WEBHOOK_URL hardcoded in Dockerfile builder ENV (acceptable for client-bundle env vars)
- Image tag: `easypanel/posgresql/web_g2:latest` matches EasyPanel's naming convention
- Did NOT use Vercel (user objected explicitly in previous session)
</decisions_made>

<blockers>

- **Browser DNS cache**: Chrome still resolves g2intelligence.co to 72.62.87.218 (old nginx). Fix: `chrome://net-internals/#dns` → Clear host cache, then `chrome://net-internals/#sockets` → Flush socket pools. OR use incognito window. OR wait ~4 hours for TTL to expire.
- **Visual diff vs Vercel**: Vercel's h1 computed at 32px, EasyPanel at 72px for same lg:text-7xl class. Likely Vercel was deployed from older code or has different responsive behavior. Not a blocker for correctness.
</blockers>

<context>
Server infrastructure is correct. The problem at pause time is purely browser-side DNS cache. Once that's cleared, the full Next.js site should be visible. Key server facts for next session:
- VPS IP: 82.25.86.23, SSH alias: easypanel-vps
- Swarm service: posgresql_web_g2
- Build dir: /tmp/web_g2_build (GitHub clone, can git pull + rebuild anytime)
- Traefik config: /etc/easypanel/traefik/config/main.yaml
- Container at pause: 2c3bc510ccbc (easypanel/posgresql/web_g2:latest)
- EasyPanel project path: /etc/easypanel/projects/posgresql/web_g2/code/
</context>

<next_action>
Start with: Ask user to clear browser DNS cache (chrome://net-internals/#dns → Clear host cache) OR open g2intelligence.co in incognito. Then verify title reads "G2 Intelligence — Inteligencia Artificial para Ventas y Automatización en Cali". Then run Lighthouse.
</next_action>
