---
phase: 05-performance-launch-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sitio-g2-nextjs/src/app/page.tsx
  - sitio-g2-nextjs/next.config.ts
autonomous: false
requirements: [MIGR-04, PERF-01, PERF-02]

must_haves:
  truths:
    - "npm run build exits 0 with all static routes generated"
    - "Vercel deployment URL returns HTTP 200"
    - "sitemap.xml accessible on deployed URL (HTTP 200)"
    - "robots.txt shows Allow: / on deployed URL"
    - "No webhook URLs visible in page source or browser DevTools network requests to /"
    - "All 6 sections (Hero, Servicios, Nosotros, Equipo, Contacto, Footer) render on the deployed URL"
  artifacts:
    - path: "sitio-g2-nextjs/src/app/page.tsx"
      provides: "Home page with ISR revalidation enabled"
      contains: "export const revalidate = 3600"
    - path: "sitio-g2-nextjs/next.config.ts"
      provides: "Production-ready Next.js config"
    - path: "sitio-g2-nextjs/.vercel/project.json"
      provides: "Vercel project linkage"
  key_links:
    - from: "sitio-g2-nextjs/src/app/page.tsx"
      to: "Vercel ISR cache"
      via: "export const revalidate = 3600"
      pattern: "revalidate.*3600"
    - from: "Vercel dashboard env vars"
      to: "@n8n/chat widget runtime"
      via: "NEXT_PUBLIC_N8N_CHAT_WEBHOOK_URL"
      pattern: "NEXT_PUBLIC_N8N"

user_setup:
  - service: vercel
    why: "Environment variables must be set before forms and chat widget work in production"
    dashboard_config:
      - task: "Add environment variable NEXT_PUBLIC_N8N_CHAT_WEBHOOK_URL"
        location: "Vercel Dashboard → Project Settings → Environment Variables"
        value: "https://n8n-n8n.ektnbd.easypanel.host/webhook/1c0360f1-fe27-42a5-9d24-7b52aebe9dd2/chat"
      - task: "Add environment variable N8N_WEBHOOK_URL"
        location: "Vercel Dashboard → Project Settings → Environment Variables"
        value: "https://n8n-n8n.ektnbd.easypanel.host/webhook/1c0360f1-fe27-42a5-9d24-7b52aebe9dd2/chat"
  - service: domain-registrar
    why: "DNS cutover to g2intelligence.co requires updating zone records at the domain registrar — this cannot be automated"
    dashboard_config:
      - task: "Add CNAME record: g2intelligence.co → cname.vercel-dns.com (or A record if Vercel provides IP)"
        location: "Your domain registrar (GoDaddy / Namecheap / etc.) → DNS Zone Editor"
      - task: "Set TTL to 300 seconds before making the change for faster propagation"
        location: "Registrar DNS Zone Editor"
      - task: "Verify propagation: run nslookup g2intelligence.co and confirm it resolves to Vercel"
        location: "Terminal or https://dnschecker.org"
---

<objective>
Add ISR revalidation to page.tsx, run a clean production build to confirm zero errors and acceptable JS bundle sizes, deploy to Vercel using the CLI, then validate the live deployment end-to-end (SEO files, sections, env var security).

Purpose: This is the final phase — ship the site to production and confirm it meets all Core Web Vitals and integration requirements before DNS cutover to g2intelligence.co.

Output: Vercel preview URL, confirmed build output, verified sitemap.xml + robots.txt, and a human-reviewed checkpoint covering env vars, forms, chat, and PageSpeed Insights.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- IMPORTANT: Read node_modules/next/dist/docs/ before modifying any Next.js files.
     This project uses Next.js 16.2.2 which may have breaking API changes from training data. -->
</context>

<interfaces>
<!-- Current state of files this plan modifies. Read before editing. -->

From sitio-g2-nextjs/src/app/page.tsx (current — add revalidate before generateMetadata):
```typescript
import type { Metadata } from 'next';
import HomeClient from '@/components/HomeClient';

export async function generateMetadata(): Promise<Metadata> { ... }

export default function Home() {
  return <HomeClient />;
}
```

From sitio-g2-nextjs/next.config.ts (current — minimal, add nothing unless build errors require it):
```typescript
import type { NextConfig } from "next";
const nextConfig: NextConfig = { /* config options here */ };
export default nextConfig;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add ISR and run production build</name>
  <files>sitio-g2-nextjs/src/app/page.tsx, sitio-g2-nextjs/next.config.ts</files>
  <action>
    1. Add `export const revalidate = 3600;` as a named export to `sitio-g2-nextjs/src/app/page.tsx`, placed BEFORE the `generateMetadata` function. This enables hourly ISR for the home page (per Claude's discretion — content rarely changes in v1).

    2. Inspect `sitio-g2-nextjs/next.config.ts`. No image domains config is needed (no raster images exist — all icons are lucide-react SVGs). Leave the config as-is unless the build in step 3 produces errors that require a fix.

    3. Run the production build from the `sitio-g2-nextjs/` directory:
       ```
       cd C:/Users/guido/OneDrive/G2INNOVATION/WEB_G2/sitio-g2-nextjs && npm run build
       ```

    4. From the build output, capture and record:
       - "First Load JS" size for the `/` route (target: shared JS < 150KB)
       - All routes listed (expect 7: /, /sitemap.xml, /robots.txt, /opengraph-image, /api/webhook/n8n/contact, /api/webhook/n8n/schedule, and any others)
       - Any TypeScript or linting errors (must be zero)

    5. If build fails: read the error, fix it, and rebuild. Do NOT proceed to Task 2 if build exits non-zero.

    NOTE: Before modifying page.tsx, check `node_modules/next/dist/docs/` for the correct syntax of route segment config exports in this version of Next.js (16.2.2 may differ from training data).
  </action>
  <verify>
    <automated>cd C:/Users/guido/OneDrive/G2INNOVATION/WEB_G2/sitio-g2-nextjs && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `npm run build` exits 0 with no TypeScript errors
    - Build output lists all static routes including / (ISR), /sitemap.xml, /robots.txt
    - `export const revalidate = 3600` is present in page.tsx
    - First Load JS for / route is documented in the summary
  </done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel</name>
  <files>sitio-g2-nextjs/.vercel/project.json</files>
  <action>
    Deploy the site to Vercel production using the CLI. Run from the repo root using the --cwd flag:

    ```
    vercel --cwd C:/Users/guido/OneDrive/G2INNOVATION/WEB_G2/sitio-g2-nextjs --prod
    ```

    During the Vercel CLI interactive prompts (first-time project setup):
    - "Set up and deploy?" → Yes
    - "Which scope?" → select your personal account
    - "Link to existing project?" → No (create new) if not already linked; Yes if .vercel/project.json exists
    - "What's your project's name?" → g2intelligence-site (or accept default)
    - "In which directory is your code located?" → ./ (we are already in sitio-g2-nextjs/)
    - "Want to modify settings?" → No (auto-detect Next.js)

    After deploy completes:
    1. Capture the deployment URL (format: https://g2intelligence-site-xxx.vercel.app)
    2. Run a quick HTTP check:
       ```
       curl -I https://[deployment-url]
       ```
       Confirm: HTTP/2 200 response, Content-Type: text/html

    3. Record the deployment URL — it is needed for the human checkpoint verification steps.

    NOTE: If Vercel CLI prompts for login, run `vercel login` first, then re-run the deploy command.
    NOTE: Do NOT set env vars via CLI flags. They must be set in the Vercel dashboard (user action in checkpoint below).
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://[deployment-url-from-output]</automated>
  </verify>
  <done>
    - Vercel deployment URL is captured and returns HTTP 200
    - `.vercel/project.json` exists in sitio-g2-nextjs/ confirming project linkage
    - Deployment URL is recorded in the task summary for checkpoint use
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Env vars, forms, chat, DNS, and PageSpeed</name>
  <what-built>
    Task 1 added ISR to page.tsx and confirmed a clean production build. Task 2 deployed the site to Vercel. The deployment URL is provided above in Task 2's output.
  </what-built>
  <how-to-verify>
    Complete ALL of these steps before typing "approved":

    STEP 1 — Set environment variables in Vercel dashboard (REQUIRED before forms/chat work):
    - Go to: Vercel Dashboard → your project → Settings → Environment Variables
    - Add variable: NEXT_PUBLIC_N8N_CHAT_WEBHOOK_URL
      Value: https://n8n-n8n.ektnbd.easypanel.host/webhook/1c0360f1-fe27-42a5-9d24-7b52aebe9dd2/chat
      Environment: Production, Preview, Development (all three)
    - Add variable: N8N_WEBHOOK_URL
      Value: https://n8n-n8n.ektnbd.easypanel.host/webhook/1c0360f1-fe27-42a5-9d24-7b52aebe9dd2/chat
      Environment: Production, Preview, Development (all three)
    - After adding both vars: trigger a redeploy (Deployments tab → three dots → Redeploy) so NEXT_PUBLIC_* takes effect at build time.

    STEP 2 — Test the contact form on the deployed URL:
    - Open the deployment URL in a browser
    - Fill in the Contacto section form (nombre, email, empresa, mensaje) with real test data
    - Submit and confirm: (a) success toast appears on page, (b) n8n inbox receives the submission within 2 seconds

    STEP 3 — Test the schedule modal:
    - Click the "Agenda una llamada" (or equivalent) CTA button
    - Fill in the schedule form (nombre, email, telefono, fecha, hora)
    - Submit and confirm: n8n inbox receives a submission with type: 'scheduling'

    STEP 4 — Test the chat widget:
    - Chat button should appear in the bottom-right corner (may take 2-3 seconds to load)
    - Click to open, type a test message, confirm n8n bot responds
    - Open DevTools → Network tab → confirm chat requests go to the n8n webhook URL (not hardcoded, uses env var)

    STEP 5 — Security check (webhook URLs not exposed):
    - In the browser, right-click → View Page Source
    - Search for "easypanel" or "n8n-n8n" in the source
    - The N8N_WEBHOOK_URL (server-side only) must NOT appear anywhere in page source
    - NEXT_PUBLIC_N8N_CHAT_WEBHOOK_URL will appear in client JS (this is expected and correct)

    STEP 6 — PageSpeed Insights (requires browser):
    - Visit: https://pagespeed.web.dev/analysis?url=[your-deployment-url]
    - Run analysis for Mobile
    - Check: LCP < 2.5s (PERF-01), CLS < 0.1 (PERF-02)
    - Screenshot or note the scores — record them in your "approved" message

    STEP 7 — DNS cutover to g2intelligence.co (do this when ready to go live):
    - Log into your domain registrar (GoDaddy, Namecheap, or wherever g2intelligence.co is registered)
    - Add/update DNS record: CNAME g2intelligence.co → cname.vercel-dns.com
      (Vercel will provide the exact target in Dashboard → your project → Settings → Domains)
    - Set TTL to 300 seconds before making the change
    - After saving, wait 15-60 minutes for propagation
    - Verify: run `nslookup g2intelligence.co` and confirm it resolves to Vercel
    - Add the custom domain in Vercel: Dashboard → Settings → Domains → Add g2intelligence.co
    - Vercel will auto-provision an SSL certificate

    NOTE: DNS cutover is optional for the "approved" signal. You can approve after Steps 1-6 and do DNS separately.
  </how-to-verify>
  <resume-signal>
    Type "approved" along with the PageSpeed Insights scores (LCP and CLS values from Step 6).
    If any step fails, describe what failed so the issue can be diagnosed.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-deploy automated verification</name>
  <files></files>
  <action>
    After the human checkpoint is approved, run automated verification against the deployed URL (use the URL captured in Task 2). Replace [DEPLOYMENT_URL] with the actual Vercel URL.

    Run each check and record the result:

    1. HTTP status of the home page:
       ```
       curl -s -o /dev/null -w "%{http_code}" https://[DEPLOYMENT_URL]/
       ```
       Expected: 200

    2. sitemap.xml accessible:
       ```
       curl -s -o /dev/null -w "%{http_code}" https://[DEPLOYMENT_URL]/sitemap.xml
       ```
       Expected: 200

    3. robots.txt accessible and allows indexing:
       ```
       curl -s https://[DEPLOYMENT_URL]/robots.txt
       ```
       Expected: contains "Allow: /"

    4. OG image accessible:
       ```
       curl -s -o /dev/null -w "%{http_code}" https://[DEPLOYMENT_URL]/opengraph-image
       ```
       Expected: 200

    5. Server-side env var not in page source:
       ```
       curl -s https://[DEPLOYMENT_URL]/ | grep -c "easypanel"
       ```
       Expected: 0 (N8N_WEBHOOK_URL must not appear in HTML) or a number matching only
       the NEXT_PUBLIC_ var (which is expected in client JS). If server-only URL appears
       in the static HTML source, escalate — this is a security issue.

    Record all results in the plan summary.
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://[DEPLOYMENT_URL]/sitemap.xml</automated>
  </verify>
  <done>
    - Home page: 200
    - sitemap.xml: 200
    - robots.txt: contains "Allow: /"
    - OG image: 200
    - N8N_WEBHOOK_URL (server-side): 0 occurrences in page HTML source
  </done>
</task>

</tasks>

<verification>
All checks must pass before phase is marked complete:

1. `npm run build` exits 0 — zero TypeScript errors, all routes generated
2. `export const revalidate = 3600` present in sitio-g2-nextjs/src/app/page.tsx
3. Vercel deployment URL returns HTTP 200
4. `/sitemap.xml` returns 200 on deployed URL
5. `/robots.txt` contains "Allow: /" on deployed URL
6. `/opengraph-image` returns 200 on deployed URL
7. Human confirmed: contact form delivers data to n8n
8. Human confirmed: schedule modal delivers data to n8n
9. Human confirmed: chat widget connects to n8n
10. Human confirmed: PageSpeed Insights LCP < 2.5s, CLS < 0.1
11. N8N_WEBHOOK_URL (server-only) not visible in page HTML source
</verification>

<success_criteria>
Phase 5 complete when:

1. `npm run build` exits 0 with all static routes generated and First Load JS < 150KB
2. Vercel deployment URL is accessible (HTTP 200) — site is live
3. sitemap.xml returns 200 on deployed URL (MIGR-04, SEO-04 satisfied)
4. robots.txt shows Allow: / (SEO-05 satisfied)
5. PageSpeed Insights shows LCP < 2.5s and CLS < 0.1 (PERF-01, PERF-02 satisfied)
6. All 6 sections render on the deployed URL
7. Contact form, schedule modal, and chat widget all connect end-to-end to n8n
8. No webhook server URLs exposed in page source (security requirement from MIGR-03)
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-launch-validation/05-01-SUMMARY.md` with:
- Vercel deployment URL
- Build output: First Load JS sizes, route list
- All curl verification results (HTTP status codes)
- PageSpeed Insights scores (LCP, CLS values — from human checkpoint)
- DNS status (whether cutover to g2intelligence.co was completed or pending)
- Any issues encountered and how they were resolved
</output>
